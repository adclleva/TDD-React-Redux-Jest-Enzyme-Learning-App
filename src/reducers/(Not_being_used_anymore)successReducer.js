// this function is here so we wont be getting errors when setting up Redux
// not being used since we are actually creating a reducer and not just some 
// placeholder
export default (state, action) => {
  return null;
}